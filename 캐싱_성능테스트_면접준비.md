# 캐싱 적용 + 성능테스트 면접 준비 (콘텐츠 상세 조회 API)

## STAR 요약

**Situation**
콘텐츠 상세조회 API에서 인기 콘텐츠일수록 여러 사용자가 동시에 같은 리소스를 반복 조회하는데, 매 요청마다 동일한 결과를 위해 RDS까지 왔다갔다함. 트래픽이 늘어날수록 DB 요청 수가 그대로 비례해서 늘어날 것으로 예측됨.

**Task**
DB 요청 수가 트래픽에 비례해서 늘어나는 구조를 개선해, 동시 조회자가 늘어도 DB 부하는 늘지 않도록 만들어야 했음.

**Action**
- ElastiCache(Redis) + Spring `@Cacheable`로 콘텐츠 상세를 캐싱(TTL 30분)
- 캐싱 효과를 검증하기 위해 k6로 부하테스트 진행
  - 로컬 환경 + 단일 콘텐츠 반복 조회로 처음 테스트 → 로컬 DB가 loopback이라 지연이 거의 없어 캐싱 효과가 잘 안 보임
  - 실제 배포 환경(RDS+ElastiCache)으로 전환 + 콘텐츠 여러 개를 랜덤 분산 요청(SharedArray)하는 방식으로 재설계 → 단일 콘텐츠만 반복 조회하면 캐시 히트율 100%에 수렴하는 비현실적 최적 시나리오이기 때문
  - 캐시 OFF 상태 재현 시 Redis를 직접 내리면 "연결 장애"와 "캐싱 미적용"이 섞여 결과가 왜곡되는 문제 발견 → `NoOpCacheManager` 기반 `nocache` Spring 프로파일을 설계해 캐시 계층만 순수하게 우회하도록 통제 (캐시 여부라는 변수 하나만 바꾸는 통제된 실험)

**Result**
다중 콘텐츠 분산 요청 기준(50 VU, 30초, 배포 환경)
- 캐시 ON: avg 75.13ms / p95 187.61ms / RPS 282.57
- 캐시 OFF: avg 81.45ms / p95 247.32ms / RPS 273.38
- 개선율: p95 24.1% 감소, avg 7.8% 감소, RPS 3.4% 증가
- 평균보다 tail latency(느린 요청) 완화에 캐싱이 더 크게 기여한다는 인사이트 확보

---

## 예상 꼬리질문

**1. DB 부하가 발생하는 문제가 뭔데요?**
RDS 인스턴스는 CPU, 커넥션 풀(HikariCP 기본 10개), 디스크 I/O 같은 자원이 유한하다. 동일한 콘텐츠를 여러 사용자가 동시에 조회하면, 매 요청마다 커넥션 하나를 점유하고 CPU 사이클을 써서 "이미 답이 정해진" 쿼리를 매번 새로 실행하는 것. 트래픽이 늘면 이 유한한 자원을 두고 요청들이 경쟁하게 되고, 커넥션 풀이 꽉 차면 뒤의 요청들은 커넥션을 기다리며 지연되고, CPU가 포화되면 이 쿼리와 무관한 다른 요청들까지 같이 느려지는 연쇄 효과가 생긴다. db.t3/t4g.micro 같은 프리티어급 소형 인스턴스라 이 한계에 더 빨리 도달할 수 있는 구조였다.

**2. 캐싱이 뭔데요?**
자주 조회되지만 조회 비용이 있는 데이터를, DB보다 훨씬 빠른 저장소(Redis, 인메모리 key-value 스토어)에 임시로 복사해두는 것. 요청이 오면 먼저 캐시를 확인하고(`@Cacheable`), 있으면 그대로 반환(cache hit), 없으면 DB에서 가져와서 캐시에 저장한 뒤 반환(cache miss)한다. 데이터가 바뀌면 해당 캐시를 무효화(`@CacheEvict`)해서 다음 조회 때 최신 값을 다시 채우게 하고, TTL을 걸어 무효화를 놓쳐도 일정 시간 후엔 자동 갱신되도록 안전장치를 뒀다.

**3. 다른 방법은 없었을까요?**
- RDS Read Replica로 읽기 트래픽 분산 — 인스턴스 추가 비용, 여전히 매번 DB(replica)까지 가야 해서 인메모리 캐시만큼의 속도 이득은 없고, 복제 지연으로 인한 정합성 이슈도 생김
- 로컬 인메모리 캐시(Caffeine 등) — 네트워크 왕복이 없어 더 빠르지만, 앱 인스턴스를 여러 대로 스케일아웃하면 인스턴스마다 캐시가 따로 놀아서 정합성이 깨짐. Redis처럼 인스턴스 간 공유되는 분산 캐시가 확장성 면에서 더 안전
- 쿼리 최적화/인덱싱 — 이미 PK 조회라 최적화 여지가 거의 없고, "매번 반복 실행"이라는 근본 문제는 그대로 남음
- 이미 구축된 ElastiCache 인프라도 있었고, 확장성과 속도를 함께 잡을 수 있는 분산 캐시(Redis)를 선택

**4. 캐시 전/후를 왜 그렇게 복잡하게 테스트했나요? 그냥 전에 한 번, 후에 한 번 실행하면 되잖아요.**
Redis를 그냥 꺼서 "전" 상태를 만들면 안 됐다. Redis가 죽어있으면 연결 시도 자체가 타임아웃(수백 ms~초 단위)이 나면서 "캐싱이 없어서 느린 것"과 "Redis 연결 실패로 지연이 생긴 것"이 섞여 비교가 왜곡된다. 그래서 Redis는 그대로 살려두고, `NoOpCacheManager`로 캐시 로직 자체만 우회하는 `nocache` 프로파일을 따로 만들어서, 캐싱 여부라는 변수 하나만 바꾸고 나머지 조건(인프라, 네트워크, DB)은 전부 동일하게 유지했다. 통제된 실험(controlled experiment)을 만들려면 비교하려는 변수 하나만 바꿔야 하는데, Redis를 그냥 끄는 건 이 원칙을 깨는 방법이었다.

**5. 왜 cache-aside 방식으로 했어요? 그 방식이 뭔데요? 다른 방법은 없었어요?**

*cache-aside(look-aside)란*: 애플리케이션이 캐시와 DB를 직접 관리하는 방식. 읽기 시 캐시를 먼저 확인해서 없으면(miss) DB에서 읽어와 캐시에 채우고(lazy loading), 반환한다. 쓰기 시에는 DB에 쓰고 캐시는 무효화(evict)만 한다 — 캐시에 새 값을 바로 갱신하지 않고 지워서, 다음 읽기 때 자연스럽게 최신값으로 다시 채워지게 한다.

*왜 이걸 썼나*: Spring Cache 추상화(`@Cacheable`/`@CacheEvict`)가 기본으로 이 패턴을 지원해 구현이 간단했고, 우리 데이터 특성(읽기 많고 쓰기 적음)에 잘 맞았다. 필요한 데이터만 캐싱하는 lazy 방식이라 콜드 데이터에 메모리를 낭비하지 않는다. DB가 항상 원본(source of truth)으로 남아있어서, 캐시가 날아가도(Redis 재시작 등) 데이터 유실 없이 다음 요청에서 다시 채워진다 — 캐시는 언제든 버리고 다시 만들 수 있는 휘발성 사본이라 안정성이 높다.

*다른 방법*:
- write-through: 쓸 때 캐시와 DB를 동기적으로 함께 갱신. 캐시가 항상 최신이지만 모든 쓰기가 캐시를 거쳐야 해서 쓰기 지연이 늘고 캐시가 critical path에 들어감. 쓰기 빈도가 낮은 우리 데이터엔 과한 실시간성.
- write-behind(write-back): 캐시에 먼저 쓰고 비동기로 나중에 DB에 반영. 쓰기 처리량은 좋아지지만 캐시가 DB에 반영되기 전에 죽으면 데이터 유실 위험이 있고 큐/재시도 등 구현이 복잡함. 우리 서비스 규모엔 과한 설계.
- read-through: 캐싱 로직이 애플리케이션 코드가 아니라 캐시 레이어/라이브러리 안에 내장되어 투명하게 동작. `@Cacheable`도 결과적으론 비슷하지만, 키 구조/TTL/무효화 시점을 직접 설계했다는 점에서 cache-aside에 더 가깝다.

---

## 추가 꼬리질문 (p95 / RPS 기초 개념)

**6. p95가 뭔가요? 왜 평균이 아니라 p95를 봤어요?**

p95는 전체 요청을 응답시간 순으로 줄 세웠을 때, 가장 느린 5%를 제외한 지점의 값 — 즉 "요청의 95%가 이 시간 안에 끝난다"는 지표. 평균(avg)은 절대다수인 빠른 요청들에 의해 값이 낮게 눌리기 때문에, 일부 사용자가 실제로 체감하는 "느린 케이스"를 가려버림. 캐시가 정말 도움이 되는 구간은 오히려 이 tail(꼬리, 느린 케이스)이라 — DB 커넥션 풀 대기, GC, 네트워크 지연이 겹치는 순간처럼 "운 나쁜 요청"일수록 캐시 히트가 그 지연을 건너뛰게 해주는 효과가 큼. 실제로 우리 결과에서도 avg는 7.8% 개선인데 p95는 24.1% 개선으로 훨씬 크게 나온 게 이 근거임 — 평균보다 tail에서 캐싱 효과가 더 크게 드러난다는 뜻.

**7. RPS는 뭔가요? 왜 지연시간(p95/avg) 말고 이것도 같이 봤어요?**

RPS(Requests Per Second)는 서버가 초당 처리한 요청 수 — 즉 처리량(throughput) 지표. 지연시간(latency)과 처리량(throughput)은 서로 다른 축이라 하나만 보면 오해가 생길 수 있음: 예를 들어 서버가 요청을 대기열에 쌓아두고 천천히 처리하면 지연시간은 나빠지지만 RPS는 유지될 수도 있고, 반대로 요청을 빨리 실패(에러)시키면 지연시간은 좋아 보여도 실제 처리량은 준 것일 수 있음. 그래서 "느려지지 않으면서 처리량도 늘었다"를 같이 보여줘야 캐싱이 진짜 이득이라는 걸 입증할 수 있어서 RPS도 같이 측정함. 우리 결과에서 RPS가 282.57(캐시 ON) vs 273.38(캐시 OFF)로 3.4% 늘어난 건, 같은 30초 동안 같은 부하 조건에서 서버가 더 많은 요청을 처리해낼 수 있었다는 뜻.

**8. 50 VU, 30초면 충분한 표본인가요?**

RPS와 duration으로 총 요청 수를 역산하면: 캐시 ON은 282.57 RPS × 30초 ≈ 8,477건, 캐시 OFF는 273.38 RPS × 30초 ≈ 8,201건. p95는 상위 5%(느린 순 정렬 기준)를 기준으로 계산되는데, 약 8천 건이면 상위 5%만 해도 400건 안팎이라 이상치 한두 개에 흔들릴 정도로 작은 표본은 아님. VU 50개를 30초간 유지 부하로 돌린 것도, 순간 스파이크가 아니라 "지속된 부하 상태에서의 안정적인 지연시간 분포"를 보려는 의도였음 (그래서 ramp-up 없이 처음부터 50 VU 고정으로 시작하는 constant-VU 방식을 씀).

---

## 배포환경 DB 조회 횟수 재현 (2026-08-27 추가)

**왜 했나**

이력서엔 "(로컬 재현) DB 조회 횟수 13,667건→36건(99.7%↓)"이라고 적혀있는데, 이건 배포 서버에 계측 로직이 없어서 로컬에 트래픽 패턴을 재현해 측정한 수치였다. 배포환경에서도 같은 효과가 실제로 나는지 직접 확인해보기로 함.

**방법**

- `ContentRepositoryImpl.findById`에 임시로 `AtomicInteger` 카운터 추가 + 5초마다 로그 출력, `compose.prod.yaml`의 `SPRING_PROFILES_ACTIVE`에 `nocache`를 임시로 얹어 캐시 계층을 통째로 우회하도록 함(로컬 실험 때 쓴 것과 같은 `NoOpCacheManager` 방식)
- GitHub Actions를 `workflow_dispatch`로 수동 트리거해 EC2에 배포(재시작으로 카운터 0부터 시작)
- k6 `stampede-test.js`로 **한 번도 요청된 적 없는 콜드 콘텐츠 하나**에 30개 동시요청을 보내고, 배포서버 로그(`docker logs`)에서 카운터가 몇 증가했는지 확인
- 캐시 ON/OFF 두 조건 모두 재배포(컨테이너 재시작)해서 카운터를 0으로 리셋한 뒤 측정 — 같은 프로세스 안에서 이어서 재면 이전 테스트의 잔여 카운트가 섞여 오염되므로

**결과**

- nocache(캐시 우회) 상태: 콜드 콘텐츠 1개에 동시요청 30개 → DB 히트 30개 (요청 수와 1:1, 예상대로)
- 캐시 ON(`@Cacheable(sync=true)`) 상태: 서로 다른 콜드 콘텐츠 2개로 각각 독립 시행 → **둘 다 DB 히트 4개** (30개 중 26개가 캐시 스탬피드 방지로 걸러짐, 86.7%↓)
- 두 번 독립 시행에서 동일하게 4가 나와, 우연(타이밍 노이즈)이 아니라 재현 가능한 현상으로 판단

**이론값(1)과의 차이 — 원인 확정 및 수정 완료 (2026-08-27)**

`@Cacheable(sync=true)`가 이상적으로 동작하면 동시요청은 첫 번째 스레드만 DB를 타고 나머지는 그 결과를 기다렸다 재사용해서 히트가 1이어야 하는데, 실측은 4였다. Spring Data Redis 공식 문서와 `RedisCache.java`/`DefaultRedisCacheWriter.java` 소스를 직접 확인해 원인을 확정했다: `RedisCache.get(key, valueLoader)`는 자체 동기화 없이 그대로 `RedisCacheWriter.get(...)`에 위임하는데, 기본(non-locking) writer는 `doGet()`이 null이면 곧장 `valueLoader.get()`(DB 조회)을 실행한다 — 즉 `sync=true`를 붙여도 Redis 레벨에서 concurrent miss를 막아주는 코드가 애초에 없었다. "4"는 첫 스레드의 `doPut()`이 반영되기 전 짧은 타이밍 창 안에 `doGet()`을 이미 통과해버린 요청 수였다.

**수정**: `CacheConfig`의 `RedisCacheManager.builder(connectionFactory)`를 `RedisCacheManager.builder(RedisCacheWriter.lockingRedisCacheWriter(connectionFactory))`로 교체 — SETNX 기반 락으로 concurrent miss를 실제로 직렬화하도록 함.

**트레이드오프**: 이 락은 키 단위가 아니라 "캐시 이름" 단위(`content~lock`)라, 서로 다른 콘텐츠 A/B가 동시에 cold-miss여도 하나가 기다려야 한다. content 캐시는 트래픽이 크지 않아 감수 가능하다고 판단했고, 트래픽이 커지면 per-key 락(Redisson 등)으로 교체가 필요함을 인지하고 있음.

**재검증**: 수정 배포 후 새로운 cold 콘텐츠 1개로 동일한 k6 stampede-test(30개 동시요청) 재실행 → DB 조회 1건으로 수렴 확인 (`[CacheTest] DB hit count so far: 1`).

**정리**

측정 끝나고 임시 계측 코드(`AtomicInteger` 카운터, `@Scheduled` 로그)와 `nocache` 프로파일 감지 로직을 커밋으로 제거하고 재배포함 — 프로덕션 코드에 디버그용 임시 코드를 남겨두지 않기 위함. (2026-08-27 원인 재조사 때 검증용으로 카운터를 잠깐 다시 넣었다가, 1로 수렴 확인 후 다시 제거함)

---

## 추가 예상 꼬리질문 (배포환경 재현 관련)

**9. 로컬 재현 수치(13,667→36, 99.7%)랑 배포환경 실측 수치(30→4→1, 86.7%→96.7%)가 왜 다른가요?**

(참고: 배포환경 수치는 최초 측정 시 4였다가, 원인 규명·수정 후 1로 재검증됨 — 자세한 건 Q10 참고)

두 테스트는 조건 자체가 다르다. 로컬은 지속적인 트래픽 패턴(여러 콘텐츠에 분산된 요청을 30초 이상 지속)을 흉내낸 수치고, 배포환경 건 "콜드 콘텐츠 하나에 순간적으로 몰리는 30개 동시요청"이라는 스탬피드 시나리오 하나만 잘라서 본 것이다. 모수(요청 수, 지속시간, 콘텐츠 분산도)가 다르니 절대적인 감소율을 1:1로 비교할 수치가 아니고, 각각 다른 질문("지속 트래픽에서 캐시가 DB 부하를 얼마나 줄이나" vs "동시 폭주 한 번에 캐시 스탬피드 방지가 얼마나 먹히나")에 답하는 별개의 실험으로 보는 게 맞다.

**10. 왜 30개 중 4개가 DB까지 갔나요? 완전히 막은 거 아니었어요?**

처음 실측했을 땐 4였다. 원인을 소스 레벨로 확인해보니, 기본(non-locking) `RedisCacheWriter` 조합에서는 `@Cacheable(sync=true)`를 붙여도 Redis 레벨에서 concurrent miss를 막아주는 로직이 전혀 없었다 — `RedisCache.get(key, valueLoader)`가 그대로 writer에 위임되고, non-locking 구현은 캐시 미스면 곧장 DB 조회를 실행하기 때문. `RedisCacheWriter.lockingRedisCacheWriter`로 교체해 SETNX 기반 락을 걸었고, 재배포 후 같은 테스트를 다시 돌려서 DB 조회가 1건으로 수렴하는 걸 직접 확인했다. 다만 이 락은 키 단위가 아니라 캐시 이름 단위라, 서로 다른 콘텐츠가 동시에 cold-miss면 그 사이엔 여전히 대기가 발생한다 — 지금 트래픽 규모에선 감수할 만한 트레이드오프로 판단했다.

**11. 이런 임시 계측 코드를 실제 운영 서버에 배포해도 되나요?**

이번엔 개인 프로젝트라 실사용자가 없어서 감수했지만, 원칙적으로는 위험한 방식이다. `nocache` 프로파일을 켜는 순간 그 배포 기간 동안은 실제 방문자 트래픽도 캐시 없이 전부 DB로 직행하게 되고, 실제로 테스트 도중 원인을 알 수 없는 요청 824건이 DB에 그대로 꽂히는 걸 관찰하기도 했다(어디서 온 요청인지는 요청 단위 로깅이 없어 특정 못함). 운영 서비스였다면 스테이징 환경을 별도로 두거나, 저트래픽 시간대에 롤백 계획을 세워두고 짧게 진행했을 것이다.

---

## TTL 30분을 선택한 이유

처음엔 직관적으로 30분 정도면 적당하다고 봤는데, 다시 점검해보니 이게 설명 가능한 근거를 갖추고 있었다. `@CacheEvict`가 이미 리뷰 등록이나 배치 갱신 시 즉시 캐시를 지우니까, 실제 데이터 신선도는 그 즉시 무효화가 담당하고 있고 TTL은 그 무효화가 어떤 이유로 실패했을 때(예: Redis 순간 장애)를 대비한 안전장치에 가깝다. 너무 짧으면 캐시 재적재가 잦아져 캐싱 효과가 줄고, 너무 길면 안전장치로서 의미가 없어지니, 콘텐츠 데이터가 초 단위로 급변하는 성격이 아니라는 점을 감안해서 30분 정도면 충분하다고 판단했다.

(참고: 콘텐츠 배치 동기화 주기도 30분인데, 이건 위 판단 근거와는 무관한 우연의 일치라 면접 답변에는 넣지 않기로 함 — 인과관계 없는 디테일을 굳이 얘기하면 "의도한 거냐"는 불필요한 꼬리질문만 생김)
